# 2282 - sort
# Read 5 digits and output them sorted

# Read 5 characters into memory
A<I
[0]<A
A<I
[1]<A
A<I
[2]<A
A<I
[3]<A
A<I
[4]<A

# Bubble sort: compare and swap
# Compare [0] and [1], swap if needed
A<[0]
B<[1]
# If A > B, swap
# Use mem[256+A] = 1, mem[256+B] = 0
# After setting, check if mem[256+B] == 1
[256]<0
# Set positions 256 through 511 to 0 first would take too long
# Use different approach: check if A > B by testing value ranges

# For ASCII '0'-'9' (48-57), create lookup to determine order
# Actually, just compare: if B > A, swap

# Set mem[B] = 1
[0]<0
[48]<0
[49]<0
[50]<0
[51]<0
[52]<0
[53]<0
[54]<0
[55]<0
[56]<0
[57]<0
[B]<1

# Check mem[A]
C<[A]
# If C==0, then A < B or A==B, no swap needed
# If C==1, then A > B (A==B case), swap needed

# Actually this check: [B]=1, if [A]==1, then A==B (no swap)
# If [A]==0, then A!=B. But which is larger?
# Since we only set [B]=1, [A]==1 means A>=B, [A]==0 means A<B

# Wait, I set [B]=1. Then [A] is 1 only if A==B.
# If A<B, [A] is still whatever it was (0).
# If A>B, [A] is still whatever it was (either 0 or 1 from previous).

# This approach doesn't work for comparison. Let me use different method.

# Alternative: count occurrences of each digit and output in order
[48]<0
[49]<0
[50]<0
[51]<0
[52]<0
[53]<0
[54]<0
[55]<0
[56]<0
[57]<0

# Count each
D<[0]
E<[D]
E<1
[D]<E

D<[1]
E<[D]
E<1
[D]<E

D<[2]
E<[D]
E<1
[D]<E

D<[3]
E<[D]
E<1
[D]<E

D<[4]
E<[D]
E<1
[D]<E

# Now mem[48-57] contains count of each digit

# Output digits in order
# For each digit '0'-'9', output it count times

# But we need loops, which we don't have directly
# We can use the program loop with state

# Actually, let's just do a simple sort with explicit comparisons

# Simpler: since only 5 digits, use selection sort explicitly
# Find min of all 5, output it, mark as used, repeat

# Or even simpler: try all 5! = 120 permutations? No, too many.

# Let me just use the counting approach and explicit output

# Count for '0' (48)
A<[48]
# Output '0' A times
# But how to loop A times?

# Use state: mem[500] = current digit to output, mem[501] = count remaining
[500]<48  # Start with '0'
[501]<[48]  # Count

# In loop: if count > 0, output and decrement, else move to next digit

Z<1  # For now just halt
